*** Begin Patch
*** Update File: index.html
@@
-    <script type= module>
-      import { initializeApp } from https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js;
-      import { getAuth, signInWithEmailAndPassword, onAuthStateChanged, signOut } from https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js;
-      import { getFirestore, collection, addDoc, doc, getDoc, setDoc, updateDoc, deleteDoc, onSnapshot, query, orderBy, limit } from https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js;
-
-      const firebaseConfig = {
-        apiKey: AIzaSyCL0bCUl0dJFO2AZg17PMVb4jR8u_nSwn4,
-        authDomain: vale-admin-a3c51.firebaseapp.com,
-        projectId: vale-admin-a3c51,
-        storageBucket: vale-admin-a3c51.firebasestorage.app,
-        messagingSenderId: 42562595285,
-        appId: 1:42562595285:web:6665e51bd4447d5f8a355c
-      };
-
-      const app = initializeApp(firebaseConfig);
-      const auth = getAuth(app);
-      const db = getFirestore(app);
-
-      window.firebaseAuth = {
-        signInWithEmailAndPassword,
-        onAuthStateChanged,
-        signOut,
-        _auth: auth
-      };
-
-      window.firebaseDB = {
-        collection: (name) => collection(db, name),
-        add: addDoc,
-        doc: doc,
-        get: getDoc,
-        set: setDoc,
-        update: updateDoc,
-        delete: deleteDoc,
-        onSnapshot,
-        query,
-        orderBy,
-        limit
-      };
-    </script>
-
-    <script src=admin.js></script>
-  </body>
-</html>
+    <script type= module>
+      import { initializeApp } from https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js;
+      import { getAuth, signInWithEmailAndPassword, onAuthStateChanged, signOut } from https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js;
+      import {
+        getFirestore,
+        collection,
+        addDoc,
+        doc,
+        getDoc,
+        setDoc,
+        updateDoc,
+        deleteDoc,
+        onSnapshot,
+        query,
+        orderBy,
+        limit
+      } from https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js;
+
+      const firebaseConfig = {
+        apiKey: AIzaSyCL0bCUl0dJFO2AZg17PMVb4jR8u_nSwn4,
+        authDomain: vale-admin-a3c51.firebaseapp.com,
+        projectId: vale-admin-a3c51,
+        storageBucket: vale-admin-a3c51.firebasestorage.app,
+        messagingSenderId: 42562595285,
+        appId: 1:42562595285:web:6665e51bd4447d5f8a355c
+      };
+
+      const app = initializeApp(firebaseConfig);
+      const auth = getAuth(app);
+      const db = getFirestore(app);
+
+      window.firebaseAuth = {
+        signInWithEmailAndPassword: (email, password) =>
+          signInWithEmailAndPassword(auth, email, password),
+        onAuthStateChanged: (callback) => onAuthStateChanged(auth, callback),
+        signOut: () => signOut(auth),
+        _auth: auth
+      };
+
+      window.firebaseDB = {
+        collection: (name) => ({
+          name,
+          add: (data) => addDoc(collection(db, name), data),
+          doc: (id) => ({
+            get: () => getDoc(doc(db, name, id)),
+            set: (data) => setDoc(doc(db, name, id), data),
+            update: (data) => updateDoc(doc(db, name, id), data),
+            delete: () => deleteDoc(doc(db, name, id))
+          }),
+          orderBy: function(field, direction = asc) {
+            this._orderBy = { field, direction };
+            return this;
+          },
+          limit: function(count) {
+            this._limit = count;
+            return this;
+          },
+          onSnapshot: function(onNext, onError) {
+            let q = collection(db, name);
+            if (this._orderBy) {
+              q = query(q, orderBy(this._orderBy.field, this._orderBy.direction));
+            }
+            if (this._limit) {
+              q = query(q, limit(this._limit));
+            }
+            return onSnapshot(
+              q,
+              (snapshot) => {
+                onNext({
+                  docs: snapshot.docs.map((d) => ({
+                    id: d.id,
+                    data: () => d.data()
+                  }))
+                });
+              },
+              onError
+            );
+          }
+        })
+      };
+    </script>
+    <script src=admin.js></script>
+  </body>
+</html>
*** End Patch
